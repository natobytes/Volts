name: Validate Wraps Content

on:
  pull_request:
    branches: [main]
    paths:
      - 'content/_wraps/**'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      - run: npm run validate -- --collection wraps
