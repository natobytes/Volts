name: Validate Hornsounds Content

on:
  pull_request:
    branches: [main]
    paths:
      - 'content/_hornsounds/**'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - run: npm run validate
